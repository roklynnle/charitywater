/* Beginner-friendly CSS using your brand color palette and a bold, modern font */

/* Set up brand colors as CSS variables for easy use */
:root {
  --misty-gray: #CBCDD1;
  --nature-green: #7BAE7F;
  --soft-blue: #77ABBC;
  --deep-navy: #003366;
  --rich-black: #1A1A1A;
  --bold-yellow: #FFC907;
}

/* Use a bold, modern sans-serif font for the whole page */
body {
  font-family: 'Proxima Nova', Arial, Helvetica, sans-serif;
  background-color: var(--misty-gray); /* Light gray background */
  color: var(--rich-black);            /* Dark text for readability */
  margin: 0;
  padding: 0;
}

/* Example: Style for a header section */
header {
  background-color: var(--deep-navy);  /* Deep navy background */
  color: var(--bold-yellow);           /* Bold yellow text */
  text-align: center;
  padding: 2em 1em;
}

/* Example: Style for a button */
.button {
  background-color: var(--bold-yellow); /* Bright yellow button */
  color: var(--rich-black);             /* Black text */
  border: none;
  border-radius: 24px;                  /* Pill shape */
  padding: 1em 2em;
  font-weight: bold;
  text-transform: uppercase;
  cursor: pointer;
  margin: 1em 0;
}

/* Example: Section backgrounds */
.section-green {
  background-color: var(--nature-green);
  color: var(--rich-black);
  padding: 2em 1em;
}

.section-blue {
  background-color: var(--soft-blue);
  color: var(--rich-black);
  padding: 2em 1em;
}

/* Responsive design for mobile devices */
@media (max-width: 600px) {
  header, .section-green, .section-blue {
    padding: 1em 0.5em;
  }
  .button {
    width: 100%;
    padding: 1em 0;
  }
}

/* 
  We use @media (max-width: 600px) to make the page look good on phones.
  This changes padding and button width so everything fits on small screens.
*/